# Existing dashboard-created services keep their current settings.
# This file is also ready for a new Render Blueprint deployment.
services:
  - type: web
    name: mistra-fiste
    runtime: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
