# Optional: if you already have an NVE HydAPI key, the server will use it first.
# Without a key, Mistra Fiske automatically tries NVE VEPS/HYDRA.
# NVE_API_KEY=your-free-key
# MET_USER_AGENT=MistraFiske/1.2 (contact: your-email)
PORT=3000
